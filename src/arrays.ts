let numbers: number[] = [1, 2, 3, 4, 5, 6]; //declare and initilise value at same time
let names: string[]; //declare the variable assign value later

numbers.forEach((n) => console.log(n.toString()));

names = ["padmanabha", "stringssss"]; //assign value later

console.log(names);
